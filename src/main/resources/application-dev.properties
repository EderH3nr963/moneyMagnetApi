spring.datasource.url=jdbc:postgresql://localhost:5432/moneyMagnet
spring.datasource.username=meuUser
spring.datasource.password=minhasenha123

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=update

logging.level.org.springframework.security=DEBUG
app.observability.db-query-count.enabled=true

api.security.token.secret=morango_e_banana
api.security.refresh-token.expiration-days=30
api.security.refresh-token.cookie-secure=false

app.frontend.base-url=http://localhost:3000

springdoc.swagger-ui.enabled=true
springdoc.api-docs.enabled=true

spring.mail.host=${EMAIL_HOST}
spring.mail.port=${EMAIL_PORT:587}
spring.mail.username=${EMAIL_USERNAME}
spring.mail.password=${EMAIL_PASSWORD}
spring.mail.properties.mail.debug=${EMAIL_DEBUG:false}

pluggy.base-url=${PLUGGY_BASE_URL:https://api.pluggy.ai}
pluggy.client-id=${PLUGGY_CLIENT_ID}
pluggy.client-secret=${PLUGGY_CLIENT_SECRET}
pluggy.api-key=${PLUGGY_API_KEY:}
